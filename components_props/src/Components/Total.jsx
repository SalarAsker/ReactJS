

const Total = (props) =>{
    // console.log(props)
    return(
        <>
            <p>Total exercises in the course {props.exercise1 + props.exercise2 + props.exercise3}</p> 
        </>
    )
}

export default Total